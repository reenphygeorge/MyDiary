<div class="container">
    <h1 class="text-center main-head mt-5">View</h1>
    <div class="row mt-5 d-flex justify-content-center">
        {{#each jsondiary}}
        <div class="card diary-view-card mb-5" style="width: 50rem;">
            <div class="card-body">
                <h6>{{this.date}}</h6>
                <div>{{{this.data}}}</div>
            </div>
        </div>
        {{/each}}
    </div>
</div>